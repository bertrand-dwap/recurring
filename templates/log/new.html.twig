{% extends 'base.html.twig' %}

{% block title %}{{ recurringTask.task }}{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1 class="h4 mb-0">{{ recurringTask.task }}</h1>
            <a href="{{ path('app_recurring_task_index') }}" class="btn btn-outline-secondary">
                <i class="bi bi-arrow-left me-1"></i> Revenir à la liste
            </a>
        </div>

        {{ include('log/_form.html.twig', { 'button_label': 'Faire la tâche' }) }}
    </div>
{% endblock %}
